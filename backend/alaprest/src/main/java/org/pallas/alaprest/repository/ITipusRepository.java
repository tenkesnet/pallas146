package org.pallas.alaprest.repository;

import org.pallas.alaprest.model.Tipus;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ITipusRepository extends JpaRepository<Tipus,Integer> {
}
